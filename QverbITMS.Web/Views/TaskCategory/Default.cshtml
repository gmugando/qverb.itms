@model IEnumerable<QverbITMS.Core.Domain.TaskCategory>

@{
    ViewBag.Title = "Index";
}


<!-- jQuery -->
<script type="text/javascript" charset="utf8" src="//code.jquery.com/jquery-1.10.2.min.js"></script>

<script type="text/javascript">
    $(function () {
        $('#txTable').dataTable({
            "bPaginate": true,
            "bLengthChange": true,
            "bFilter": false,
            "bSort": true,
            "bInfo": true,
            "bAutoWidth": false
        });
    });

</script>

<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                @*<h3 class="box-title">Incident Categories List</h3>*@
                <div class="box-tools">
                    <div class="input-group">
                        <input type="text" name="table_search" class="form-control input-sm pull-right" style="width: 150px;" placeholder="Search" />
                        <div class="input-group-btn">
                            <button class="btn btn-sm btn-default"><i class="fa fa-search"></i></button>
                        </div>
                    </div>
                </div>
            </div><!-- /.box-header -->
            <div class="box-body table-responsive no-padding">
                <table class="table table-hover" id="txTable">
                    <thead>
                        <tr>
                            <th>Category</th>
                            <th>Active</th>
                            <th>Description</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    @if (@item.Active)
                                    {
                                        <span class="label label-success">Active</span>;
                                    }
                                    else
                                    {
                                        <span class="label label-warning">Inactive</span>;
                                    }
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Descr)
                                </td>
                                <td>
                                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div><!-- /.box-body -->
        </div><!-- /.box -->
    </div>
</div>

<div class="box-footer ">
    <a class="btn btn-primary" href="@Url.Action("Create", "TaskCategory")">
        Add
    </a>
</div>

@section styles{
    @Styles.Render("~/Content/themes/adminlte/css/dataTables")
}

@section scripts{
    @Scripts.Render("~/bundles/adminlte/plugins/dataTable")
}


